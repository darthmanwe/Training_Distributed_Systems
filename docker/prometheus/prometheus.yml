global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "dist-rl-platform"
    static_configs:
      - targets: ["host.docker.internal:8000"]
        labels:
          service: "dist-rl-trainer"
